[build]
  publish = "public"
    functions = "netlify/functions"
    [functions]
      node_bundler = "esbuild"
      [[redirects]]
        from = "/bot*"
          to = "/.netlify/functions/tg/:splat"
            status = 200
